#include "main.h"

/**
 * _isupper - checks uppercase characters
 * @c: character to be checked
 * Return: 1 if c is uppercase, o for otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
