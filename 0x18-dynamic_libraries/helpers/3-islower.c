#include "main.h"

/**
 * _islower - looks for lowercase characters
 *@c: character to be looked for.
 * Return:0 if c is lowercase, o otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
